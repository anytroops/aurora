[pytest]
testpaths = tests
# The suite runs in seconds locally; anything past this is a hang, not slowness.
# Failing with a traceback beats a CI job that spins for hours.
timeout = 120
timeout_method = thread
filterwarnings =
    ignore::DeprecationWarning
